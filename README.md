# CarbHEALTH — MVP

Quick start (after installing dependencies):

```bash
npm install
npm run dev
```

This is a small Vite + React SPA stub containing the UI and calculator utilities described in `plan.md`.
# CarbHealth