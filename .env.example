# Example environment variables — copy this to `.env` and fill values for local dev
VITE_APP_TITLE=CarbHEALTH
VITE_INITIAL_USERS=1247
VITE_INITIAL_SUGAR_SAVED=34.5
VITE_ENABLE_TRIVIA=true
VITE_API_URL=https://api.example.com
# Add your AI provider API key here for server-side calls (do NOT expose this to the client)
OPENAI_API_KEY=
# Sanity (GROQ) configuration — add your project id, dataset and server token
SANITY_PROJECT_ID=
SANITY_DATASET=
GROQ_API_KEY=
